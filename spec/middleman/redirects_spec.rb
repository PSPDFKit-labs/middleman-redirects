require 'spec_helper'

describe Middleman::Redirects do
  it 'has a version number' do
    expect(Middleman::Redirects::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
